// Stub for CSS imports pulled in transitively by component barrels (e.g. the
// Carousel's react-responsive-carousel stylesheet). Jest cannot parse CSS, and
// styles are irrelevant to behavioral assertions.
module.exports = {};
